#include<stdio.h>
//#include <string.h>
int  main(){
  char str[15],strrev;

  printf("Enter string: ");
  gets(str);
  printf("String is: %s",str);
  strrev(str);
  printf("\nReverse String is: %s",str;
 return 0;
}
